// 예측 가능하고 디버깅 하기 쉬운!!
// 연산자 우선 순위 ((x + y) * z)

몸무게 % (신장 * 신장);

if ((isLogin && token) || user)

let number;

function decrement() {
  number--; // 지양하기

  number = number - 1 // 지향하기
}

function increment() {
  number++; // 지양하기

  number = number + 1 // 지향하기
}